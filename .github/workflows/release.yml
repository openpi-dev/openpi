name: Release

on:
  workflow_dispatch:
    inputs:
      tag:
        description: Existing version tag to publish (for example, v0.5.0)
        required: true
        type: string
  push:
    tags:
      - "v*.*.*"

concurrency:
  group: release-${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}
  cancel-in-progress: false

permissions:
  contents: read
  id-token: write

jobs:
  release:
    name: Release
    uses: openpi-dev/automation/.github/workflows/openpi-release.yml@1bb1c3fb521d1bd93cf9274070cce2ad9ffcb274
    with:
      tag: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}
